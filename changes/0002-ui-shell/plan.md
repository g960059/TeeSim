# Plan

1. Create the change pack and define the shared UI/store contract.
2. Implement app entry points, constants, stub case data, and Zustand state with derived view matching.
3. Implement the layout shell and UI components against the shared store.
4. Wire keyboard shortcuts and responsive pane/tab behavior in the app shell.
5. Run typecheck and fix any integration issues.
