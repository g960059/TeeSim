# Plan

1. Create the `src/core/` module and define the public types.
2. Implement minimal vector and matrix math primitives.
3. Implement centerline interpolation, shaft/tip/transducer transforms, and imaging-plane derivation.
4. Implement weighted nearest-preset view matching and ADR score bands.
5. Add unit tests and `vitest.config.ts`, then run `npx vitest run`.
