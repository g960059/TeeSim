# Plan

1. Add an isolated Vite spike entrypoint and npm script.
2. Implement a synthetic cardiac-like 256x256x256 `vtkImageData` generator plus CPU/GPU reslice pipelines.
3. Add wedge masking, attenuation, UI controls, and findings/timing reporting.
4. Verify the spike compiles and the dev server starts with the standalone config.
